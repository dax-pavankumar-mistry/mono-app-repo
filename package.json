{
  "name": "mono-app-repo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:full": "yarn workspace full-app dev",
    "dev:cut": "yarn workspace cut-app dev",
    "build:full": "yarn workspace full-app build",
    "build:cut": "yarn workspace cut-app build",
    "build:ui": "yarn workspace @daxwell/ui build"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}
